@model IEnumerable<ShopDoCu.Models.Coupon>
@{
    ViewData["Title"] = "Quản lý mã giảm giá";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h3 class="fw-bold text-primary mb-0">🎟️ Quản lý Mã giảm giá</h3>
            <p class="text-muted small mb-0">Quản lý các chương trình khuyến mãi toàn sàn</p>
        </div>
        <a asp-action="CreateCoupon" class="btn btn-primary px-4 shadow-sm">
            <i class="bi bi-plus-lg me-2"></i>Tạo mã mới
        </a>
    </div>

    <div class="card border-0 shadow-sm mb-4">
        <div class="card-body p-3">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <form method="get" action="/Admin/Coupons" class="d-flex">
                        <div class="input-group">
                            <span class="input-group-text bg-white border-end-0"><i class="bi bi-search text-muted"></i></span>
                            <input type="text" name="searchString" class="form-control border-start-0 ps-0" placeholder="Tìm theo mã code..." value="@ViewBag.SearchString">
                            <button type="submit" class="btn btn-outline-primary">Tìm kiếm</button>
                        </div>
                    </form>
                </div>
                <div class="col-md-6 text-end">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success d-inline-block py-1 px-3 mb-0">
                            <i class="bi bi-check-circle me-1"></i> @TempData["SuccessMessage"]
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                    <tr class="text-uppercase small text-muted">
                        <th class="ps-4">Mã Code</th>
                        <th>Mức giảm</th>
                        <th>Điều kiện</th>
                        <th>Số lượng</th>
                        <th>Thời gian</th>
                        <th>Trạng thái</th>
                        <th class="text-end pe-4">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            // Logic kiểm tra trạng thái
                            bool isExpired = DateTime.Now > item.EndDate;
                            bool isSoldOut = item.Quantity <= 0;
                            bool isActive = item.IsActive && !isExpired && !isSoldOut;

                            <tr>
                                <td class="ps-4">
                                    <div class="d-flex align-items-center">
                                        <div class="bg-light rounded p-2 me-3 text-center border" style="width: 45px; height: 45px;">
                                            <i class="bi bi-ticket-perforated-fill text-primary fs-5"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0 fw-bold text-danger">@item.Code</h6>
                                            <small class="text-muted">ID: #@item.CouponId</small>
                                        </div>
                                    </div>
                                </td>

                                <td>
                                    @if (item.DiscountType == "Percent")
                                    {
                                        <span class="badge bg-info text-dark shadow-sm">Giảm @item.DiscountValue.ToString("N0")%</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning text-dark shadow-sm">Giảm @item.DiscountValue.ToString("N0")đ</span>
                                    }
                                </td>

                                <td>
                                    <small class="text-muted">Đơn tối thiểu:</small><br>
                                    <span class="fw-bold text-dark">@item.MinOrderAmount.ToString("N0")đ</span>
                                </td>

                                <td>
                                    @if (item.Quantity > 10)
                                    {
                                        <span class="text-success fw-bold">@item.Quantity</span>
                                    }
                                    else if (item.Quantity > 0)
                                    {
                                        <span class="text-warning fw-bold">Còn @item.Quantity</span>
                                    }
                                    else
                                    {
                                        <span class="text-danger fw-bold">Hết mã</span>
                                    }
                                </td>

                                <td>
                                    <div class="d-flex flex-column small">
                                        <span class="text-success"><i class="bi bi-play-circle me-1"></i>@item.StartDate.ToString("dd/MM/yyyy")</span>
                                        <span class="text-danger"><i class="bi bi-stop-circle me-1"></i>@item.EndDate.ToString("dd/MM/yyyy")</span>
                                    </div>
                                </td>

                                <td>
                                    @if (!item.IsActive)
                                    {
                                        <span class="badge bg-secondary">Đã tắt</span>
                                    }
                                    else if (isExpired)
                                    {
                                        <span class="badge bg-danger">Hết hạn</span>
                                    }
                                    else if (isSoldOut)
                                    {
                                        <span class="badge bg-dark">Hết lượt</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success">Đang chạy</span>
                                    }
                                </td>

                                <td class="text-end pe-4">
                                    <a asp-action="EditCoupon" asp-route-id="@item.CouponId" class="btn btn-sm btn-outline-primary me-1" title="Sửa">
                                        <i class="bi bi-pencil-square"></i>
                                    </a>
                                    <a asp-action="DeleteCoupon" asp-route-id="@item.CouponId" 
                                       class="btn btn-sm btn-outline-danger" 
                                       onclick="return confirm('Bạn có chắc chắn muốn xóa mã @item.Code không?');" title="Xóa">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-center py-5">
                                <img src="https://cdn-icons-png.flaticon.com/512/4076/4076432.png" width="60" class="opacity-50 mb-3">
                                <p class="text-muted">Chưa có mã giảm giá nào được tạo.</p>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>